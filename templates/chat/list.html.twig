{% extends 'base.html.twig' %}

{% block body %}
{{ parent() }}
  <h2>Elige con quién chatear</h2>

  <ul>
      {% for user in users %}
          <li>
              <a href="{{ path('chat', { id: user.id }) }}">
                  {{ user.username }}
              </a>
          </li>
      {% endfor %}
  </ul>
{% endblock %}
